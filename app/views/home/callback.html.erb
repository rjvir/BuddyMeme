Your top friends<hr><br />
<!--<%= @top_friends %>-->
<%= @meUID %>

<% @top_friends["query2"].each do |top| %>
	<% if top["id"]!=@meUID %>
		<%= top["name"] %> 
		<%= top["id"] %> 
		<hr>
	<% end %>
<% end %>
<br>
All your friends<hr>

<ul>
<!--f["name"]-->
<% @graph_data.each do |f| %>
<%= link_to f["name"], photos_path(:id => f["id"]) %> <hr>
<% end %>
</ul>